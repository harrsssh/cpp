#include<bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[])
{
    // we take n+1 sized array in case of char array to store null element at the end

    // char arr[100] = "apple";

    // int i=0;
    // while(arr[i] != '\0'){
    //     cout << arr[i];
    //     i++;
    // }

    // taking input

    char arr1[100];
    cin >> arr1;

    cout << arr1;

    return 0;
}
